#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>
#include <stdint.h>
#include <sys/types.h>
int main() {
  pid_t pid;
  pid = fork();
  if(pid != 0 ){
    printf("Parent");
  }
  else{
    printf("Child");}
  return 0;
}
